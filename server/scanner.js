const os = require('os');
const ping = require('net-ping');

async function scan() {
    // Use net-ping to discover devices
}

function getInterface() {
    // Get Network interfaces
} 

export default scan;